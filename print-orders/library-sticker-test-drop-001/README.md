# Library Sticker Print Test - Drop 001

Use `individual-pdfs/` for the print order if the library wants one file per sticker.
Use `full-city-sticker-print-test-all.pdf` if the library wants one multi-page PDF.

Every sticker has a distinct QR code with `drop=001`, `asset`, `source`, `kind`, `break`, and `line` parameters.
Scan each printed page before ordering the full run.

The source SVGs are in `source-svg/` for future edits; do not upload them unless the printer explicitly accepts SVG.